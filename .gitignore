# Logs and world saves — generated at runtime, don't commit
.logs/
saves/

# Playit binaries, runtime state, logs, and credentials
.playit/*
!.playit/.gitkeep
playit.conf

# OS junk
.DS_Store
Thumbs.deploy
